package com.instabug.library.session;

import androidx.annotation.IntRange;
import androidx.annotation.NonNull;
import java.util.List;

public interface a {
    @NonNull
    List a(@NonNull List list, @IntRange(from = 1) int i11);
}
