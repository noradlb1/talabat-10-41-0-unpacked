package com.instabug.survey.utils;

import androidx.annotation.NonNull;
import com.instabug.survey.models.Survey;

public interface l {
    void a(@NonNull Survey survey);

    void b(@NonNull Survey survey);
}
