package library.talabat.mvp.rateorder;

import library.talabat.mvp.IGlobalListener;

public interface RateOrderListener extends IGlobalListener {
    void onOrderRatedSuccussfully(boolean z11, float f11);
}
